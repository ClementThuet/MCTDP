{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{%endblock%}

{% block body %}
    {{parent()}}
    <div class='container-fluid'>
        <div class='row'>
            <div class='col-md-12'>
                <h2>Historique des séances de Qi Gong </h2>
               
                <table class="table table-striped table-light table-bordered table-hover tableCenter">
                        <thead class="thead-dark"> 
                            <tr>
                                <th>Patient</th>
                                <th> Date de la visite</th>
                            </tr>
                        </thead>
                        <tbody >
                            {% for seanceQG in listSeances %}
                                <tr>
                                    <td> {{seanceQG.patient.nom}} {{seanceQG.patient.prenom}} </td>
                                    <td>{{seanceQG.date|date('d-m-Y')}} </td>
                                </tr>
                            </a>
                            {%endfor%}
                        </tbody>
                    </table>
                    <div id ='btRetour' >
                        <a href="{{ path('menu_QiGong')}}">
                            <input type="button"  class="btn btn-primary" value="Retour">
                        </a>
                    </div>
{%endblock%}