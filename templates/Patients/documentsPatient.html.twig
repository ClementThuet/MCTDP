{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{%endblock%}

{% block body %}
    {{parent()}}
    <div class='container-fluid'>
        <div class='row'>
            <div class='col-md-12'>
                <h1 class="titresH1">Documents de {{ patient.nom}} {{ patient.prenom}}</h1>
               
                <table class="table table-striped table-light table-hover tableCenter">
                        <thead class="thead-dark"> 
                            <tr>
                                <th>Intitulé</th>
                                <th>Date</th>
                                <th>Observations</th>
                                <th>Visualiser</th>
                                <th>Supprimer</th>
                            </tr>
                        </thead>
                        <tbody >
                            {% for document in listDocuments %}
                                <tr>
                                    <td>{{document.intitule}} </td>
                                    <td>{{document.date|date('d-m-Y')}} </td>
                                    <td>{{document.observations}}</td>
                                    <td><a href="../../uploads/{{document.dossier}}/{{document.path}}" target="_BLANK"> <i class="fas fa-eye glyphMenu" ></i></a> </td>
                                    <td><a href='{{ path('supprimer_document',{'idDocument':document.id}) }}'> <i class=" fas fa-trash-alt glyphMenu" ></i></a> </td>
                                </tr>
                            </a>
                            {%endfor%}
                            
                        </tbody>
                    </table>
                    <div class="MenuBasCentre" >
                        <a href="{{ path('fiche_patient',{'id':patient.id})}}">
                            <input type="button"  class="btn btn-primary" value="Retour">
                        </a>
                        <a href="{{ path('ajouter_document',{'idPatient':patient.id})}}">
                            <input type="button"  class="btn btn-primary" value="Ajouter un document">
                        </a>
                    </div>
            </div>
        </div>
    </div>

{%endblock%}