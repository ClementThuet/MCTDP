{% extends 'layout.html.twig' %}

{% block stylesheets %}
    {{parent()}}
{%endblock%}

{% block body %}
    {{parent()}}
    <div class='container-fluid'>
        <div class='row'>
            <div class='col-md-12'>
                <h1 class='titresH1'>Historique des visites </h1>
                {%if rechercheEffectuee is defined %}
                    <div class='center'>
                        <button class="btn btn-primary" onclick="document.location = '{{path('historique_visites',{'page':1})}}';">Réinitialiser recherche</button>
                    </div><br>
                {%endif%}
                <table class="table table-striped table-light table-hover tableCenter">
                    <thead class="thead-dark"> 
                        <tr>
                            <th>Id<br>
                            <input type="text" class="recherche" id="searchId" placeholder="Chercher par identifiant">
                            <a href="" id="btSearchId"> <button class='btRecherche' >Rechercher</button></a></th>
                            <th> Patient<br>
                            <input type="text" class="recherche" id="searchNomPatient" placeholder="Chercher par patient">
                            <a href="" id="btSearchNomPatient"> <button class='btRecherche' >Rechercher</button></a></th>
                            <th>Date de la visite<br>
                            <input type="text" class="recherche" id="searchDate" placeholder="Chercher par date">
                            <a href="" id="btSearchDate"> <button class='btRecherche' >Rechercher</button></a></th>
                            <th>Motif de la visite<br>
                            <input type="text" class="recherche" id="searchMotif" placeholder="Chercher par motif">
                            <a href="" id="btSearchMotif"> <button class='btRecherche' >Rechercher</button></a></th>
                            <th>Observations<br>
                            <input type="text" class="recherche" id="searchObs" placeholder="Chercher par observation">
                            <a href="" id="btSearchObs"> <button class='btRecherche' >Rechercher</button></a></th>
                            <th>Modifier</th>
                            <th>Supprimer</th>
                        </tr>
                    </thead>
                    <tbody >
                        {% if listPatientsVisites is defined%}
                            {% for VisitesPatient in listPatientsVisites %}
                                {% for visite in VisitesPatient %}
                                    <tr class='pointerHover' onclick="document.location = '{{ path('fiche_visite', { 'idVisite':visite.id }) }}'">
                                        <td> {{visite.id}} </td>
                                        <td> {{visite.patient.nom}}</td>
                                        <td>{{visite.date|date('d-m-Y')}} </td>
                                        <td>{{visite.motif}} </td>
                                        <td>{{visite.observations}}</td>
                                        <td><a href='{{ path('editer_visite',{'idVisite':visite.id}) }}'> <i class="fas fa-edit glyphMenu" ></i></a></td>
                                        <td><a href='{{ path('supprimer_visite',{'idVisite':visite.id}) }}'> <i class=" fas fa-trash-alt glyphMenu"</a> </td>
                                    </tr>
                                </a>
                                {%endfor%}
                            {%endfor%}
                            {% else %}
                                {% for visite in listVisites %}
                                    <tr class='pointerHover' onclick="document.location = '{{ path('fiche_visite', { 'idVisite':visite.id }) }}'">
                                        <td>{{visite.id}} </td>
                                        <td>{{visite.patient.nom}}</td>
                                        <td>{{visite.date|date('d-m-Y')}} </td>
                                        <td>{{visite.motif}} </td>
                                        <td>{{visite.observations}}</td>
                                        <td><a href='{{ path('editer_visite',{'idVisite':visite.id}) }}'> <i class="fas fa-edit glyphMenu" ></i></a></td>
                                        <td><a href='{{ path('supprimer_visite',{'idVisite':visite.id}) }}'> <i class=" fas fa-trash-alt glyphMenu"</a> </td>
                                    </tr>
                                </a>
                                {%endfor%}
                        {%endif%}
                    </tbody>
                </table>
                {%if html is defined %}
                <div class='row'>
                    <div class='col-md-12  '>
                        <div class="">{{html|raw}}</div>
                    </div>
                </div>
                {%endif%}
                <div class='MenuBasCentre' >
                    <a href="{{ path('menu_patients',{'page':1})}}">
                        <input type="button"  class="btn btn-primary" value="Retour">
                    </a>
                </div>
            </div>
        </div>
    </div>
{%endblock%}
{% block javascripts %}
       {{parent()}}
        <script type="text/javascript" src="http://code.jquery.com/jquery-latest.min.js"></script> 
        <script>
        //Recherche sur nom
               $("#searchId").on('keyup', function() { // everytime keyup event
                   value=$('#searchId').val();
                   idPatient=$('#idPatient').html();
                   console.log(idPatient);
                   var pathToRedirect ='{{path('rechercher_visite',{'Entite':'Visite','champ':'id','valeur':'Erreur'})}}';
                   var finalPath=pathToRedirect.replace("Erreur", value);
                   document.getElementById("btSearchId").href=finalPath; 
               });
               
                $("#searchNomPatient").on('keyup', function() { // everytime keyup event
                   value=$('#searchNomPatient').val();
                   idPatient=$('#idPatient').html();
                   console.log(idPatient);
                   var pathToRedirect ='{{path('rechercher_visite',{'Entite':'Visite','champ':'nomPatient','valeur':'Erreur'})}}';
                   var finalPath=pathToRedirect.replace("Erreur", value);
                   document.getElementById("btSearchNomPatient").href=finalPath; 
               });

               //Recherche sur date
               $("#searchDate").on('keyup', function() { // everytime keyup event
                   value=$('#searchDate').val();
                   var pathToRedirect ='{{path('rechercher_visite',{'Entite':'Visite','champ':'date','valeur':'Erreur'})}}';
                   var finalPath=pathToRedirect.replace("Erreur", value);
                   document.getElementById("btSearchDate").href=finalPath; 
               });

               //Recherche sur motif
               $("#searchMotif").on('keyup', function() { // everytime keyup event
                   value=$('#searchMotif').val();
                   var pathToRedirect ='{{path('rechercher_visite',{'Entite':'Visite','champ':'motif','valeur':'Erreur'})}}';
                   var finalPath=pathToRedirect.replace("Erreur", value);
                   document.getElementById("btSearchMotif").href=finalPath; 
               });

               //Recherche sur Observation
               $("#searchObs").on('keyup', function() { // everytime keyup event
                   value=$('#searchObs').val();
                   var pathToRedirect ='{{path('rechercher_visite',{'Entite':'Visite','champ':'observations','valeur':'Erreur'})}}';
                   var finalPath=pathToRedirect.replace("Erreur", value);
                   document.getElementById("btSearchObs").href=finalPath; 
               });
               </script>
{%endblock%}